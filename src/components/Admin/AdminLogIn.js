import React from 'react';


const AdminLogin = ({setAdminToken}) => {

    //log in api call should return false if isAdmin returns false



    return (
        <>This should be a simple log in form accessing (and checking) for Admin credentials</>
    )
}

export default AdminLogin